# Changelog

## V1.0.11.0 — 2026-08-31

### Final release

- 此版本替代并重新发布此前的 V1.0.11.0；V1.0.12.0 Release 已撤回，不再作为对外版本。
- 后续以 GitHub Release 的便携式单文件 EXE 作为唯一分发与自动更新来源。

### Added

- **跨文件夹命名对比**：可选择已有命名素材目录，按大小写不敏感的文件名集合检查冲突。
- **输出目录选择**：可将新批次目标统一写入指定文件夹，未指定时仍在源目录处理。
- **冲突自动递增**：自动避开输出目录、当前批次和对比目录中的重名目标。
  - 数字尾段严格尝试 `T1` 到 `T100`。
  - 自定义尾段使用 `补手机`、`补手机_2`、`补手机_3` 等可读形式。
- **仅高清导出**：只做 1080p 导出而不改变原文件名，支持原位覆盖或生成 `_1080p` 副本。

### Changed

- 移除 Inno Setup 安装包功能、安装器构建脚本、安装器语言资源和旧安装包产物；后续仅分发便携式单文件 EXE。
- 保留 GitHub 自动更新：下载发布的裸 EXE、校验 SHA-256、退出旧进程后自动替换，并清理下载文件和替换脚本。
- 命名预览在执行前统一检查目标存在、批次内重复、对比目录重名和目标占用。
- 外部输出/导出成功后同步更新 UI 行模型路径；取消或失败时保留源文件。
- 启动画面固定等待 3 秒，删除每次启动的图标轮换；关于窗口不再显示标题栏图标。
- 导出区文案调整为“仅高清”和“高清命名”。

### Removed

- 视频播放器功能及 VLC/WMP/WebBrowser 相关实现、抽帧与缩略图预览机制。
- 启动图标轮换源码和 9 个启动图标资源。

### Verification

- `SelfTest 86/86`：通过。
- `SmokeTest OK`：通过。
- `构建EXE.ps1`：通过全部构建门和产物校验。
## V1.0.9.0 — 2026-08-13

### Added

- 授权系统：激活码验证、试用期和到期提示。
- `LicenseManager`：纯函数验证与 DPAPI 加密存储。

### Changed

- 状态文本集中管理：`PlanStatusText.FormatStatusDetail()` 统一格式化。
- 授权密钥工具：`生成授权密钥.ps1`。

## V1.0.8.0 — 2026-07-27

### Added

- 自动更新检测与下载（GitHub Releases）。
- 更新提示对话框，支持跳过版本。
- 启动图标轮换系统（9 个图标，每次启动随机选择）。

### Changed

- 应用名称统一为 VideoRenamer（之前为 MaterialRenamer）。
- 启动画面显示 1 秒最小时长。
- 发布流程同时提供安装包和裸 EXE。

### Fixed

- 清除列表时释放旧预览缓存。
- 工作区徽章左对齐。
- 启动图标正确解码（从 ICO 提取最大 PNG 帧）。

### Security

- 更新清单必须匹配 VideoRenamer 应用标识。
- 更新清单必须包含有效的 SHA-256 哈希。
- 下载的更新在替换前进行哈希验证。

### Upgrade note

旧版本的应用标识和本地存储不会迁移。请卸载旧版本后全新安装 V1.0.8.0。