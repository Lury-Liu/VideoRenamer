# Changelog

## V1.0.12.0 — 2026-08-29

### Added

- **跨文件夹命名对比**：可选择已有命名素材目录，按大小写不敏感的文件名集合检查冲突。
- **输出目录选择**：可将新批次目标统一写入指定文件夹，未指定时仍在源目录处理。
- **冲突自动递增**：自动避开输出目录、当前批次和对比目录中的重名目标。
  - 数字尾段严格尝试 `T1` 到 `T100`。
  - 自定义尾段使用 `补手机`、`补手机_2`、`补手机_3` 等可读形式。
- **导出-only**：只做 1080p 导出而不改变原文件名，支持原位覆盖或生成 `_1080p` 副本。

### Changed

- 命名预览在执行前统一检查目标存在、批次内重复、对比目录重名和目标占用。
- 外部输出/导出成功后同步更新 UI 行模型路径；取消或失败时保留源文件。
- 文档统一补充 E/S/镜号、T 自动管理和目录边界说明。

### Verification

- `SelfTest 89/89`：通过。
- `SmokeTest OK`：通过。
- 6 个构建门和正式 EXE 产物校验：通过。

## V1.0.11.0 — 2026-08-29

### Added

- **跨文件夹对比**：可指定一个已有命名素材目录，预览时按文件名（大小写不敏感）检查重名。
- **输出目录选择**：可将本批次重命名/导出目标统一写入指定目录；不指定时保持在源文件所在目录处理。
- **冲突自动递增**：开启后自动避开输出目录、当前批次和对比目录中的同名目标。
  - 默认数字尾段按 `T1` → `T2` → … → `T100` 尝试。
  - 自定义尾段按 `补手机` → `补手机_2` → `补手机_3` 等形式尝试。
  - `T100` 以内没有可用名称时，继续显示冲突并阻止执行，避免静默覆盖素材。
- **导出-only 流程**：可以只做 1080p 导出而不改变原文件名，支持原位覆盖或生成 `_1080p` 副本。

### Changed

- 命名预览现在同时考虑目标目录、当前计划、对比目录和文件占用状态。
- 高清导出使用总体进度和取消支持；导出成功后，外部输出目录中的路径会同步回 UI 行模型。
- 详情面板简化为纯信息显示：文件名、大小、分辨率、时长和路径。
- MediaLoadScheduler 简化为单队列。
- 删除播放器及抽帧预览相关命名残留，回归“命名 + 导出”的核心范围。
- 自测用例更新为 **89 个**。

### Removed

- 视频播放器功能及所有 VLC/WMP/WebBrowser 相关实现。
- `VideoFrameStripProvider`、`VideoThumbnailProvider`、`ThumbnailCache` 抽帧/缩略图预览机制。
- 播放器运行时、播放器测试和相关诊断脚本。

### Verification

- `SelfTest 89/89`：通过。
- `SmokeTest OK`：通过。
- `构建EXE.ps1`：通过全部构建门和产物校验。
- 本地单文件 EXE：`dist\VideoRenamer.exe`，文件版本 `1.0.11.0`，大小约 104 MB。

### Rationale

命名冲突和跨目录管理是当前素材整理的核心需求；播放器与抽帧预览多次尝试后仍不稳定，因此保留稳定的素材信息读取和 FFmpeg 导出能力，减少运行时依赖并降低维护成本。

## V1.0.9.0 — 2026-08-13

### Added

- 授权系统：激活码验证、试用期和到期提示。
- `LicenseManager`：纯函数验证与 DPAPI 加密存储。

### Changed

- 状态文本集中管理：`PlanStatusText.FormatStatusDetail()` 统一格式化。
- 授权密钥工具：`生成授权密钥.ps1`。

## V1.0.8.0 — 2026-07-27

### Added

- 自动更新检测与下载（GitHub Releases）。
- 更新提示对话框，支持跳过版本。
- 启动图标轮换系统（9 个图标，每次启动随机选择）。

### Changed

- 应用名称统一为 VideoRenamer（之前为 MaterialRenamer）。
- 启动画面显示 1 秒最小时长。
- 发布流程同时提供安装包和裸 EXE。

### Fixed

- 清除列表时释放旧预览缓存。
- 工作区徽章左对齐。
- 启动图标正确解码（从 ICO 提取最大 PNG 帧）。

### Security

- 更新清单必须匹配 VideoRenamer 应用标识。
- 更新清单必须包含有效的 SHA-256 哈希。
- 下载的更新在替换前进行哈希验证。

### Upgrade note

旧版本的应用标识和本地存储不会迁移。请卸载旧版本后全新安装 V1.0.8.0。