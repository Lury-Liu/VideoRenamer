# Project Status — VideoRenamer

**Status date:** 2026-08-31
**Current source/build version:** **V1.0.11.0**
**Overall state:** 核心功能稳定；本轮已移除 Inno Setup 安装包功能，保留 GitHub 自动更新、下载校验、替换和临时文件自动清理；回归验证和 EXE 构建均通过。

| 项目 | 当前状态 |
| --- | --- |
| 项目目录 | `D:\VideoRename` |
| 本地 EXE | `dist\VideoRenamer.exe` |
| EXE 文件版本 | `1.0.11.0` |
| EXE 文件大小 | `101,919,744` bytes（约 102 MB，2026-08-31 本轮构建产物） |
| 本地更新清单 | ``发布更新到GitHub.ps1` 在发布时生成，版本与本地 EXE 一致 |
| 远程 Release | 本文只确认本地清单与构建产物；线上可用性以 GitHub Release 实际页面为准 |
| 分支约定 | 远程主分支为 `main` |
| Git 提交状态 | 当前工作树包含本轮优化修改，尚未创建新的提交 |

## 本轮代码优化进度（2026-08-31）

### 已完成

- ✅ 启动画面计时由 `4000ms` 调整为 `3000ms`，提示文案同步由“4 秒”改为“3 秒”。
- ✅ 删除启动图标轮换功能及相关初始化、状态存储、文件监听、预览和测试代码。
- ✅ 删除 `assets\startup-icons\01.ico` 至 `09.ico`，构建脚本不再收集轮换图标。
- ✅ 全局应用图标统一使用 `assets\app.ico`；`AppIcon` 保留为统一图标入口。
- ✅ 关于窗口已移除 `AppIcon.Apply(this)`，不再主动设置左上角应用图标。
- ✅ 关于窗口已在 `ShowInTaskbar = false;` 后设置 `ShowIcon = false;`，显式隐藏标题栏左上角图标。
- ✅ 导出区域文案已更新：`仅导出高清` → `仅高清`，`导出并重命名` → `高清命名`。
- ✅ 已检查 `StartupIcon`、`startup-icons`、`current.ico`，当前代码和构建文件无残留引用。
- ✅ 已删除 Inno Setup 配置、安装器构建脚本、安装器语言资源及旧安装包产物；发布物改为便携式单文件 EXE。
- ✅ GitHub 自动更新继续保留：下载 SHA-256 校验后的裸 EXE，退出旧进程后替换目标 EXE，并删除下载临时文件和替换脚本。

### 本轮验证和构建结果

- ✅ `AboutForm` 已添加 `ShowIcon = false;`。
- ✅ SelfTest：86/86 通过（删除启动图标轮换相关测试后，当前用例总数为 86）。
- ✅ SmokeTest：`SmokeTest OK`。
- ✅ `构建EXE.ps1`：6 个质量门、版本一致性和产物校验全部通过。
- ✅ `git diff --check` 无错误；启动图标关键词仅保留在交接/状态文档，源码与构建配置无残留。
- ℹ️ 本轮源码和文档修改仍未创建 Git 提交；`dist/` 是被忽略的构建产物目录。

本轮图标轮换删除涉及的源码和资源清理约 **511 行代码/资源变更**（以当前 Git diff 统计为准）。

## 已完成能力

- ✅ 批量重命名：`E{集数}-S{场号}-{镜号}{后缀}-{尾段}{扩展名}`。
- ✅ 顶部统一集数、默认场号；可按行使用场号。
- ✅ 镜号支持 1–2 位英文字母后缀并统一为大写。
- ✅ 主要素材和备用素材按当前行顺序自动生成 `T1`、`T2`……。
- ✅ 可选择对比文件夹，阻止与已有命名素材重名（文件名大小写不敏感，当前只扫描第一层）。
- ✅ 可选择输出文件夹，将全部目标集中到指定目录；不指定时保留源目录行为。
- ✅ 冲突自动递增：数字尾段最多尝试 `T1` 到 `T100`；自定义尾段使用 `_2`、`_3` 等分隔序号。
- ✅ 冲突无法解决时保持阻塞状态，不覆盖已有文件。
- ✅ 1080p 导出、总体进度、取消；支持原位覆盖和 `_1080p` 副本模式。
- ✅ 导出-only：只升级视频，不改变原文件名。
- ✅ 重命名历史记录与撤销。
- ✅ 护眼/暗色主题、免责声明、授权校验和 GitHub 自动更新（下载、替换、临时文件清理）。

## 命名与冲突决策顺序

1. 根据 E、S、镜号/后缀和素材顺序生成候选名。
2. 决定目标目录：有输出目录就使用输出目录，否则使用源文件目录。
3. 检查目标文件是否存在、当前批次是否重复、对比目录是否存在同名文件，以及目标是否被占用。
4. 未开启自动递增时，冲突显示为阻塞状态。
5. 开启自动递增时：数字尾段尝试到 `T100`；自定义尾段尝试带下划线的序号；仍无可用名称则保持阻塞。
6. 只有预览中没有阻塞项时才允许执行。

## 验证结果

- **本轮 SelfTest：86/86，通过（`SelfTest OK`）。**
- **本轮 SmokeTest：通过（`SmokeTest OK`）。**
- **本轮构建门：6 个质量门全部通过。**
- **本轮产物校验：通过；已确认内嵌 FFmpeg、单个应用图标、无额外 DLL。**
- `dist\VideoRenamer.exe` 已于 2026-08-31 重新构建：文件版本 `1.0.11.0`，大小 `101,919,744` bytes，SHA-256 为 `B7569B9D07371B40B7A738B9CEB0ED297AB4417EA343D8F3A36D9411FD4A8993`。

## 已知边界

- 对比文件夹当前只扫描第一层，不递归子目录。
- 软件不会分析视频画面来自动识别真实镜号；用户仍需提供或确认 E、S 和镜号。
- 从 `VideoRenamer.ps1` 直接运行不会嵌入 FFmpeg，因此媒体信息和导出功能会静默降级；正式使用应运行 `dist\VideoRenamer.exe`。
- 软件不再提供安装包；对外分发使用便携式单文件 `VideoRenamer.exe`，并通过 GitHub Release 保持应用内自动更新。

## 相关文档

- [README.md](README.md)：用户说明、命名规则和使用步骤。
- [CHANGELOG.md](CHANGELOG.md)：版本变更记录。
- [AGENTS.md](AGENTS.md)：构建、测试、分层和冻结契约。
- [docs/HEALTH_ASSESSMENT.md](docs/HEALTH_ASSESSMENT.md)：健康度评估。
- [handoff.md](handoff.md)：本次交接摘要与后续操作清单。